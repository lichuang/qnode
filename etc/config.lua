qnode_config = {
  -- threads config
  thread = {
    num = 5,
  },

  -- log congfig
  log = {
    path = "./log",
    level = "debug",
    handler = "file",
  },

  -- script config
  script = {
    path = "./script",
  },

  -- server config
  server = {
  }
}
