qnode_config = {
  -- threads config
  thread = {
    num = 5,
  },

  -- log congfig
  log    = {
  },

  -- script config
  script = {
    path = "./script",
  },

  -- server config
  server = {
  }
}
