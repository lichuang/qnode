qnode_config = {
  -- threads config
  thread = {
    num = 5,
  },

  -- log congfig
  log = {
    path = "./log",
    level = "debug",
  },

  -- script config
  script = {
    path = "./script",
  },

  -- server config
  server = {
    daemon = 0,
  }
}
